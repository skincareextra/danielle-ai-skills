{
  "name": "auth-engine",
  "version": "0.1.0",
  "purpose": "Reusable authentication and authorization engine for fleet projects.",
  "responsibilities": [
    "User identity lifecycle (signup, signin, password reset)",
    "Session and token management",
    "Role/permission checks",
    "OAuth provider integration points"
  ],
  "interfaces": {
    "inbound": [
      "authenticate(credentials)",
      "refresh_session(refresh_token)",
      "authorize(subject, action, resource)"
    ],
    "outbound": [
      "data-engine.userStore",
      "external.idpProviders"
    ]
  },
  "contract_file": "contract.json",
  "isolation_boundary": "Projects may call only operations defined in contract.json. Internal stores, secrets, and signing keys are private to auth-engine.",
  "notes": [
    "Scaffolded from fleet directive.",
    "GitHub repository artifacts are the source of truth for requirements and contract evolution."
  ]
}
